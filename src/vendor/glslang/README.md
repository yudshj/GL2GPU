# @webgpu/glslang Assets

This directory vendors the browser build from `@webgpu/glslang@0.0.15`:

- `glslang.js`
- `glslang.wasm`
- `glslang.d.ts`

These files are committed so runtime shader translation works without an npm
asset copy step at application startup.
